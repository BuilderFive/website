//The header is to display a stream of user's updates

/* PSEUDO CODE
    * 1. Create Header UI (should be minimizable)
    * 2. Have option to toggle group chat, 
    *    view list of other users on the platform from modal,
    *    and toggle overlay to see live updates
    * 3. Create chat message input
    */

export const Header = () => {
    return (
        <div className="h-[48px] w-screen bg-text-200">
            hi
        </div>
    )
}