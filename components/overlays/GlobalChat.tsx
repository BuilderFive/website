import React, { useEffect, useState } from 'react';

/**
 * This is a closeable overlay with an opaque background
 * It is updated by other user's messages
 * @returns 
 */
const GlobalGroupChat = () => {

    /* PSEUDO CODE
    * 1. Create overlay UI
    * 2. Create chat message UI
    * 3. Create chat message input
    * 4. Allow this component to be draggable anywhere on the screen
    */
   
};

export default GlobalGroupChat;